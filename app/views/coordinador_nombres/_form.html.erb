<%= form_for(coordinador_nombre) do |f| %>
  <% if coordinador_nombre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coordinador_nombre.errors.count, "error") %> prohibited this coordinador_nombre from being saved:</h2>

      <ul>
      <% coordinador_nombre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
