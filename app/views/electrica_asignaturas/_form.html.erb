<%= form_for(electrica_asignatura) do |f| %>
  <% if electrica_asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(electrica_asignatura.errors.count, "error") %> prohibited this electrica_asignatura from being saved:</h2>

      <ul>
      <% electrica_asignatura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :clave %>
    <%= f.text_field :clave %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
