<%= form_for(consejero) do |f| %>
  <% if consejero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consejero.errors.count, "error") %> prohibited this consejero from being saved:</h2>

      <ul>
      <% consejero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre del consejero" %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Crear", class: "btn btn-md btn-primary"%>
  </div>
<% end %>
