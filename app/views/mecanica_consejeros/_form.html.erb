<%= form_for(mecanica_consejero) do |f| %>
  <% if mecanica_consejero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mecanica_consejero.errors.count, "error") %> prohibited this mecanica_consejero from being saved:</h2>

      <ul>
      <% mecanica_consejero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre del consejero" %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="form-group">
    <%= f.submit "Crear", class: "btn btn-md btn-primary"%>
  </div>
<% end %>
