<%= form_for(cambiar_asignatura) do |f| %>
  <% if cambiar_asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cambiar_asignatura.errors.count, "error") %> prohibited this cambiar_asignatura from being saved:</h2>

      <ul>
      <% cambiar_asignatura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% if current_user.area == 'Eléctrica' %>
  <div class="form-group">
    <%= f.label :anterior, "Asignatura actual" %><br>
      <%= collection_select(:cambiar_asignatura, :anterior, ElectricaAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_anterior, ElectricaAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :actual, "Nueva asignatura" %><br>
    <%= collection_select(:cambiar_asignatura, :actual, ElectricaAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_actual, ElectricaAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>
<% elsif current_user.area == 'Electrónica' %>
  <div class="form-group">
    <%= f.label :anterior, "Asignatura actual" %><br>
      <%= collection_select(:cambiar_asignatura, :anterior, ElectronicaAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_anterior, ElectronicaAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :actual, "Nueva asignatura" %><br>
    <%= collection_select(:cambiar_asignatura, :actual, ElectronicaAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_actual, ElectronicaAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>
<% elsif current_user.area == 'Mecánica' %>
  <div class="form-group">
    <%= f.label :anterior, "Asignatura actual" %><br>
      <%= collection_select(:cambiar_asignatura, :anterior, MecanicaAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_anterior, MecanicaAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :actual, "Nueva asignatura" %><br>
    <%= collection_select(:cambiar_asignatura, :actual, MecanicaAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_actual, MecanicaAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>
<% elsif current_user.area == 'Sistemas' %>
  <div class="form-group">
    <%= f.label :anterior, "Asignatura actual" %><br>
      <%= collection_select(:cambiar_asignatura, :anterior, SistemasAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_anterior, SistemasAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :actual, "Nueva asignatura" %><br>
    <%= collection_select(:cambiar_asignatura, :actual, SistemasAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_actual, SistemasAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>
<% else %>
  <div class="form-group">
    <%= f.label :anterior, "Asignatura actual" %><br>
      <%= collection_select(:cambiar_asignatura, :anterior, TelecomAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_anterior, TelecomAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :actual, "Nueva asignatura" %><br>
    <%= collection_select(:cambiar_asignatura, :actual, TelecomAsignatura.all, :nombre, :nombre, {prompt: 'Seleccione'}) %>
  </div>

  <div class="form-group">
    <%= f.label :clave %><br>
    <%= collection_select(:cambiar_asignatura, :clave_actual, TelecomAsignatura.all, :clave, :clave, {prompt: 'Seleccione'}) %>
  </div>
<% end %>

  <div class="form-group">
    <% if can? :validar_consejero, cambiar_asignatura %>
      <%= f.label :valida_consejero %><br>
      <%= f.check_box :valida_consejero %>
    <% end %>
  </div>

  <div class="form-group">
    <% if can? :validar_coordinador, cambiar_asignatura %>
      <%= f.label :valida_coordinador %><br>
      <%= f.check_box :valida_coordinador %>
    <% end %>
  </div>

  <div class="form-group">
    <% if can? :cambiar_estado, cambiar_asignatura %>
      <%= f.label :estado %><br>
      <%= collection_select(:cambiar_asignatura, :estado, Estado.all, :estado, :estado, {prompt: 'Seleccione'}) %>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit "Crear petición", class: "btn btn-md btn-primary" %>
  </div>
<% end %>
<script>
function matchUp(selected, toselect)
{
  var idx = selected.prop('selectedIndex');
  toselect.prop('selectedIndex', idx);
}


$('#cambiar_asignatura_anterior').change(
  function() {
    matchUp($('#cambiar_asignatura_anterior'), $('#cambiar_asignatura_clave_anterior'));
  }
);

$('#cambiar_asignatura_clave_anterior').change(
  function() {
    matchUp($('#cambiar_asignatura_clave_anterior'), $('#cambiar_asignatura_anterior'));
  }
);

$('#cambiar_asignatura_actual').change(
  function() {
    matchUp($('#cambiar_asignatura_actual'), $('#cambiar_asignatura_clave_actual'));
  }
);

$('#cambiar_asignatura_clave_actual').change(
  function() {
    matchUp($('#cambiar_asignatura_clave_actual'), $('#ambiar_asignatura_actual'));
  }
);
</script>
