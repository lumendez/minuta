<%= form_for(sepi_programa) do |f| %>
  <% if sepi_programa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sepi_programa.errors.count, "error") %> prohibited this sepi_programa from being saved:</h2>

      <ul>
      <% sepi_programa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre del programa" %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
      <%= f.submit "Crear", class: "btn btn-md btn-primary"%>
  </div>
<% end %>
