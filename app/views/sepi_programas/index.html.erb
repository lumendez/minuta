<p id="notice"><%= notice %></p>

<h1>Sepi Programas</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sepi_programas.each do |sepi_programa| %>
      <tr>
        <td><%= sepi_programa.nombre %></td>
        <td><%= link_to 'Show', sepi_programa %></td>
        <td><%= link_to 'Edit', edit_sepi_programa_path(sepi_programa) %></td>
        <td><%= link_to 'Destroy', sepi_programa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sepi Programa', new_sepi_programa_path %>
