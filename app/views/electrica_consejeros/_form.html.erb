<%= form_for(electrica_consejero) do |f| %>
  <% if electrica_consejero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(electrica_consejero.errors.count, "error") %> prohibited this electrica_consejero from being saved:</h2>

      <ul>
      <% electrica_consejero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
