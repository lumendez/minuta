<%= form_for(consejero_electrica) do |f| %>
  <% if consejero_electrica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(consejero_electrica.errors.count, "error") %> prohibited this consejero_electrica from being saved:</h2>

      <ul>
      <% consejero_electrica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
