<%= form_for(electronica_consejero) do |f| %>
  <% if electronica_consejero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(electronica_consejero.errors.count, "error") %> prohibited this electronica_consejero from being saved:</h2>

      <ul>
      <% electronica_consejero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre del consejero" %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="form-group">
    <%= f.submit "Crear", class: "btn btn-md btn-primary"%>
  </div>
<% end %>
