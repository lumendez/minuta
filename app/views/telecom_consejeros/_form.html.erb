<%= form_for(telecom_consejero) do |f| %>
  <% if telecom_consejero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(telecom_consejero.errors.count, "error") %> prohibited this telecom_consejero from being saved:</h2>

      <ul>
      <% telecom_consejero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :nombre, "Nombre del consejero" %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit "Crear", class: "btn btn-md btn-primary"%>
  </div>
<% end %>
